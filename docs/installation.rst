============
Installation
============

At the command line::

    $ easy_install django-manolo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-manolo
    $ pip install django-manolo